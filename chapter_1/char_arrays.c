/* This program reads lines until the end of file.
 * It then prints the longest read line. */
#include <stdio.h>

int get_line(char line[]);

int main()
{
  // while (theres another line)
  //    if (it's longer than the previous longest)
  //      save it
  //      save it's length
  // print longest line
  char c;

}

/* Copy the input to line[] and return the length. */
int get_line(char line[])
{
  int ch, ch_count, line_length;

  while( (c = getchar()) != EOF ) {
    
}
